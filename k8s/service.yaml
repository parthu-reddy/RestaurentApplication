apiVersion: v1
kind: Service
metadata:
  name: food-delivery-service
  namespace: food-delivery-prod
  labels:
    app: food-delivery-backend
spec:
  type: ClusterIP
  selector:
    app: food-delivery-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
